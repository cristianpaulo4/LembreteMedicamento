import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:lebrete_medicamento/app/modules/telaLembrete/tela_lembrete_page.dart';

void main() {
  testWidgets('TelaLembretePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TelaLembretePage(title: 'TelaLembrete')));
    //  final titleFinder = find.text('TelaLembrete');
    //  expect(titleFinder, findsOneWidget);
  });
}
